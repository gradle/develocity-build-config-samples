pluginManagement {
 repositories {
     mavenLocal()
     gradlePluginPortal()
 }
}

plugins {
  id 'com.myorg.convention-gradle-enterprise-gradle-5-or-newer' version '1.0.0-SNAPSHOT'
}

rootProject.name = 'example-build'