pluginManagement {
 repositories {
     mavenLocal()
     gradlePluginPortal()
 }
}

plugins {
  id "com.myorg.gradle-enterprise-convensions" version "1.0.0-SNAPSHOT"
}

rootProject.name = "example-build"